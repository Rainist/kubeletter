(ns kubeletter.core-test
  (:require [clojure.test :refer :all]
            [kubeletter.core :refer :all]))

;; (deftest a-test
;;   (testing "FIXME, I fail."
;;     (is (= 0 1))))

(deftest success-test
  (testing "FIXED, I success."
    (is (= 1 1))))
